#include <stdio.h>

int main()
{
    // Do while loop

    // int num, index = 0;
    // printf("Enter a number : ");
    // scanf("%d", &num);

    // do
    // {
    //     printf("%d \n", index);
    //     index = index + 1;
    // } while (index < num);


    // while loop 
    // int i = 0;
    // while (i<54){
    //     printf("%d\n" , i);
    //     i = i + 1;

        // find  reverse of a number entered by user

        // int n , r , rev =0;

        // printf("Enter the number you want the reverse of: ");
        // scanf("%d",&n);
        // while (n!=0)      
        // {
        //     r = n % 10 ;
        //     rev = rev * 10 + r;
        //     n = n /10 ;

        // //    printf("The reverse of the number you entered is %d\n", rev);
        // }
        //    printf("The reverse of the number you entered is %d\n", rev);
        

    // }


    // for loop - everything is written with for( in this ).

    // int i,j;

    // // for ( i = 0, j=0 ; i < 5; i++)
    // // {
    // //     printf("%d %d\n",i , j);
    // // }

    // int i = 0 , j =0;
    // for (  ;i < 5; i++) // it is not necessary to write expression1 always . we can initalize it  in staring also .
    // //WE can  initialize more than one conditon at a time but the last statement will be treated as main condition or main loop other as treated as statements
    // // it is also optional but if we don't use it the loop will be became infinite loop so we have to use break statement
    // // same the expression 3 is also optional we can wirte it at end also
    // {
    //     printf("%d %d\n",i , j);
    // }
    

    return 0;
}