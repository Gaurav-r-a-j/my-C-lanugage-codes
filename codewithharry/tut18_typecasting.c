#include<stdio.h>
// typecasting syntax
// (type) value
int main(){
    int a = 3; 
    float b = (float) 54/5;
    printf("The valuse of a is %f\n", b);// here due to type conversiono we get value of 10.00 because type conversion tells about if we do anything we two int value then the operation will also in int format

    // but if we want value after point also then we use float and convert the value of float b

    // type conversion 
    // int - int = int
    // int-float = float
    // float-float = float


    return 0;
}