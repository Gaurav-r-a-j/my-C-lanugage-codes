#include <stdio.h>
#include <stdlib.h>
int main()
{
    // char str[100];
    int i = 5;
    printf("%d %d %d",i++,i++,i++);

    // scanf("%s",str);// after space it will not take or print
    // printf("%s",str);

    // gets(str);//it is used to take input string
    // puts(str);//it is used to give output of string
    return 0;
}
